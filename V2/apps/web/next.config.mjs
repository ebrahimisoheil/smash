/** @type {import('next').NextConfig} */
const nextConfig = {
  // Phase A placeholder config. Real settings (API rewrites/proxy to
  // engrave-api, image domains, etc.) land once this stops being a
  // build-verifying skeleton — see V2/docs/roadmap/14-web-application.md.
  reactStrictMode: true,
};

export default nextConfig;
