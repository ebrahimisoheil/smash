//! `smash-worker` — the Tokio job loop over `smash-core` and `smash-storage`.
//!
//! Phase A: build-verifying placeholder only. Real job claiming, leasing,
//! and handler dispatch land in later phases.

fn main() {
    println!("smash-worker: Phase A placeholder — no job loop running yet");
}
