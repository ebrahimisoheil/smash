# Personal data — never commit
raw/*
!raw/.gitkeep

wiki/sources/*
!wiki/sources/.gitkeep

wiki/concepts/*
!wiki/concepts/.gitkeep

wiki/entities/*
!wiki/entities/.gitkeep

wiki/comparisons/*
!wiki/comparisons/.gitkeep

wiki/explorations/*
!wiki/explorations/.gitkeep

# Keep index and log templates but ignore personal content
# (we ship empty versions in the repo)
wiki/_smash_schema.json

# OS junk
.DS_Store
*.swp
*~

# Python
__pycache__/
*.pyc

# Autoresearch session files
autoresearch.jsonl
autoresearch.md
autoresearch.sh
autoresearch.checks.sh
autoresearch.ideas.md
autoresearch/

# Local demo output
smash-demo/
.smash-backups/
.smash-cache/

# Local planning notes
planning/
review/

# Integration artifacts (generated by install scripts)
CLAUDE.md
AGENTS.md
GEMINI.md
GEMINI.md
.cursor/
.kiro/
.github/copilot-instructions.md
.vscode/settings.json
.smash-mcp-python

# PyPI build artifacts
mcp_package/dist/
mcp_package/*.egg-info/

# MCP registry auth tokens (personal credentials — never commit)
.mcpregistry_*
mcp_package/.mcpregistry_*

# Credential files
.env
.env.*
*.token
*.key
*.pem
*.p8
*.p12
id_rsa
id_ed25519
# Local design references
Smash Console Handoff.html

# Local editor/agent tooling
.claude/

# vhs demo render workspace
docs/media/.aha-demo/

# SmashBar build artifacts (never commit)
apps/SmashBar/.build/

# V2 Rust workspace build artifacts (Cargo.lock IS committed — only the
# compiled output isn't)
V2/target/
V2/**/target/

# V2 Next.js placeholder build artifacts
V2/apps/web/node_modules/
V2/apps/web/.next/
V2/apps/web/next-env.d.ts

# Root Rust workspace build artifacts
target/

# Local Phase B/C evidence and backup artifacts
var/
